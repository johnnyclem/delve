{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && node -e \"const f=require('path').resolve(__dirname,'..','api-zod','src','index.ts');let t=require('fs').readFileSync(f,'utf8');t=t.replace(/export \\* from .+api\\.schemas.+\\n?/g,'');require('fs').writeFileSync(f,t)\" && pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.5.2"
  }
}
